#!/usr/bin/env bash
# Setup cron jobs for the app

# Exemplo: chamar o job diário de cobrança às 08:00
# 0 8 * * * curl -s -X POST "$APP_URL/api/billing/cron/daily" -H "Cookie: session=..."

echo "Cron placeholder"
