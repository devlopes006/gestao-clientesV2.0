// Deprecated template. We now render HTML using a string-based template.
// Kept to avoid breaking imports; prefer using renderInviteEmailHtml from inviteHtml.
export { renderInviteEmailHtml } from "./inviteHtml";
